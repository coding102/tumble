<% @posts.each do |post| %>
  <div class="post_wrapper">
    <h2 class="title">
      <%= link_to post.title, post %>
      <p xlass="date">
        <%= time_ago_in_words(post.created_at) %>
      </p>
    </h2>
  </div>
<% end %>